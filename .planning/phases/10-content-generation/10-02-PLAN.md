---
phase: 10-content-generation
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/core/api/schemas/campaign_schemas.py
  - src/core/api/controllers/campaign_controller.py
  - src/core/api/app.py
autonomous: true

must_haves:
  truths:
    - "User triggers variant generation via POST endpoint and receives generated variants"
    - "User can provide custom sentiment prompts in the generation request"
    - "User can edit individual variant content via PATCH endpoint"
    - "User can review all variants with their content via existing GET endpoint"
    - "Generation endpoint returns diversity validation results"
  artifacts:
    - path: "src/core/api/schemas/campaign_schemas.py"
      provides: "Generation request/response schemas and variant edit schema"
      contains: "GenerateVariantsRequest"
    - path: "src/core/api/controllers/campaign_controller.py"
      provides: "Generation and edit endpoints on campaign router"
      contains: "generate_variants"
    - path: "src/core/api/app.py"
      provides: "ContentGenerationService initialization in lifespan"
      contains: "ContentGenerationService"
  key_links:
    - from: "src/core/api/controllers/campaign_controller.py"
      to: "src/core/api/services/content_generation_service.py"
      via: "Depends injection for generation service"
      pattern: "get_content_generation_service"
    - from: "src/core/api/controllers/campaign_controller.py"
      to: "src/core/api/schemas/campaign_schemas.py"
      via: "Request/response model imports"
      pattern: "GenerateVariantsRequest|GenerateVariantsResponse|VariantEditRequest"
    - from: "src/core/api/app.py"
      to: "src/core/api/services/content_generation_service.py"
      via: "Service initialization in lifespan"
      pattern: "_content_generation_service.*=.*ContentGenerationService"
---

<objective>
Add REST API endpoints for content generation and variant editing, wiring the ContentGenerationService into the FastAPI application.

Purpose: Exposes the generation logic via API so users can trigger variant generation, review results with diversity scores, and manually edit content before publishing. Completes the Phase 10 user workflow.

Output: Updated campaign schemas, controller with 2 new endpoints, app registration
</objective>

<execution_context>
@/home/jfr4nc0/.claude/get-shit-done/workflows/execute-plan.md
@/home/jfr4nc0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-content-generation/10-01-SUMMARY.md

@src/core/api/schemas/campaign_schemas.py
@src/core/api/controllers/campaign_controller.py
@src/core/api/app.py
@src/core/api/services/content_generation_service.py
@src/core/utils/text_diversity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generation and edit schemas</name>
  <files>src/core/api/schemas/campaign_schemas.py</files>
  <action>
Add 3 new Pydantic models to the EXISTING `src/core/api/schemas/campaign_schemas.py` file. Do NOT remove or modify existing schemas.

1. `GenerateVariantsRequest(BaseModel)`:
   - `custom_prompts: Optional[dict[str, str]] = None` -- Optional dict mapping sentiment name to custom prompt string. Example: `{"urgency": "Write with extreme urgency about AI deadlines"}`. When provided, the custom prompt overrides the preset for that specific sentiment.

2. `GenerateVariantsResponse(BaseModel)`:
   - `campaign_id: str`
   - `variants_generated: int`
   - `diversity: dict` -- Contains passed (bool), min_diversity (float), failing_pairs (list), pair_count (int)
   - `variants: list[dict]` -- Each with id, sentiment, content snippet

3. `VariantEditRequest(BaseModel)`:
   - `content: str` -- The new content text for the variant. Must be non-empty (use `pydantic.Field(min_length=1)`).

Also add a `VariantEditResponse(BaseModel)`:
   - `id: str`
   - `campaign_id: str`
   - `sentiment: str`
   - `content: str`
   - `is_selected: int`

Import `Field` from pydantic at the top if not already imported.
  </action>
  <verify>
Run: `python -c "from src.core.api.schemas.campaign_schemas import GenerateVariantsRequest, GenerateVariantsResponse, VariantEditRequest, VariantEditResponse; print('schemas OK')"` -- should import without error.

Run: `python -c "from src.core.api.schemas.campaign_schemas import VariantEditRequest; VariantEditRequest(content='test'); print('valid'); try: VariantEditRequest(content=''); except Exception as e: print(f'rejected empty: {e}')"` -- should print "valid" then "rejected empty".
  </verify>
  <done>
Four new schemas added: GenerateVariantsRequest (with optional custom_prompts), GenerateVariantsResponse (with diversity results), VariantEditRequest (with min_length=1 validation), VariantEditResponse. All existing schemas preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add generation and edit endpoints, register service in app</name>
  <files>src/core/api/controllers/campaign_controller.py, src/core/api/app.py</files>
  <action>
**In `src/core/api/controllers/campaign_controller.py`:**

Add import for the new schemas at the top (add to existing import block from campaign_schemas):
- `GenerateVariantsRequest`, `GenerateVariantsResponse`, `VariantEditRequest`, `VariantEditResponse`

Add import for ContentGenerationService:
```python
from src.core.api.services.content_generation_service import ContentGenerationService
```

Add dependency getter following the same lazy-import pattern as `get_campaign_service`:
```python
def get_content_generation_service() -> ContentGenerationService:
    from src.core.api.app import get_content_generation_service as _get
    return _get()
```

Add 2 new endpoints to the existing router:

1. `POST /api/campaigns/{campaign_id}/generate` -- Generate variant content:
   - Takes `campaign_id: str` path param, `request: GenerateVariantsRequest` body, and both services via Depends
   - Calls `content_service.generate_campaign_variants(campaign_id, custom_prompts=request.custom_prompts)`
   - Returns `GenerateVariantsResponse(**result)` with status 200
   - Error handling: ValueError -> check "not found" for 404, else 400. Generic Exception -> 500.
   - Follow exact error handling pattern from existing transition_status endpoint.

2. `PATCH /api/campaigns/{campaign_id}/variants/{variant_id}` -- Edit variant content:
   - Takes `campaign_id: str` and `variant_id: str` path params, `request: VariantEditRequest` body, `service: CampaignService = Depends(get_campaign_service)`
   - Opens a session via the campaign service's session factory: `service._session_factory()`
   - Queries CampaignVariant by id AND campaign_id (both must match)
   - If not found, raise HTTPException 404
   - Update variant.content = request.content
   - Commit session
   - Return `VariantEditResponse(id=variant.id, campaign_id=variant.campaign_id, sentiment=variant.sentiment, content=variant.content, is_selected=variant.is_selected)`
   - Wrap in try/except with HTTPException passthrough and generic 500

**In `src/core/api/app.py`:**

Add to imports:
```python
from src.core.api.services.content_generation_service import ContentGenerationService
```

Add module-level global:
```python
_content_generation_service: ContentGenerationService | None = None
```

In the `lifespan` function, add `_content_generation_service` to the global statement, and after `_campaign_service` initialization add:
```python
_content_generation_service = ContentGenerationService(_agent_db._engine)
```

Add getter function:
```python
def get_content_generation_service() -> ContentGenerationService:
    return _content_generation_service
```

No new router registration needed -- the generation endpoints are on the existing campaign_router which is already registered.
  </action>
  <verify>
Run: `python -c "from src.core.api.controllers.campaign_controller import router; routes = [r.path for r in router.routes]; print(routes); assert '/api/campaigns/{campaign_id}/generate' in ['/api/campaigns' + r.path for r in router.routes if hasattr(r, 'path')]; print('routes OK')"` -- verify generation route exists. (If the assertion is tricky due to route path format, at minimum verify the import works: `python -c "from src.core.api.controllers.campaign_controller import router; print('import OK')"`)

Run: `python -c "from src.core.api.app import get_content_generation_service; print('app getter OK')"` -- should import without error.
  </verify>
  <done>
POST /api/campaigns/{campaign_id}/generate endpoint triggers LLM content generation for all empty variants and returns diversity validation. PATCH /api/campaigns/{campaign_id}/variants/{variant_id} allows editing individual variant content. ContentGenerationService initialized in FastAPI lifespan using shared engine. Both endpoints follow established error handling patterns.
  </done>
</task>

</tasks>

<verification>
1. All new schemas import cleanly: `python -c "from src.core.api.schemas.campaign_schemas import GenerateVariantsRequest, GenerateVariantsResponse, VariantEditRequest, VariantEditResponse; print('OK')"`
2. Controller imports cleanly with new endpoints: `python -c "from src.core.api.controllers.campaign_controller import router; print('OK')"`
3. App imports cleanly with new service: `python -c "from src.core.api.app import app, get_content_generation_service; print('OK')"`
4. No existing functionality broken: `python -c "from src.core.api.schemas.campaign_schemas import CampaignCreateRequest, CampaignDetailResponse; print('existing schemas OK')"`
</verification>

<success_criteria>
- POST /api/campaigns/{campaign_id}/generate accepts optional custom_prompts and triggers variant generation
- PATCH /api/campaigns/{campaign_id}/variants/{variant_id} allows editing individual variant content with min_length=1 validation
- GenerateVariantsResponse includes diversity validation results (passed, min_diversity, failing_pairs)
- ContentGenerationService initialized in app lifespan with shared DB engine
- All existing campaign endpoints unchanged and working
- Error handling follows established patterns (ValueError -> 400/404, Exception -> 500)
</success_criteria>

<output>
After completion, create `.planning/phases/10-content-generation/10-02-SUMMARY.md`
</output>
